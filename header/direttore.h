/**
* 1. creazione risorse -> semafori, SM e code di msg
* 2. creazione processi -> erogatore_ticker (1), operatore (NOF_WORKERS) e utente (NOF_USERS)
*    2.1 Inizializzazione processi
* 3. avvio sim (SIM_DURATION giorni, N_NANO_SECS minuti)
*/

main
    risorse

    crea_processo("../bin/nomeFile", num)
    crea_processo
    crea_processo
